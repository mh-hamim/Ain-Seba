# AinSeba — deployment requirements for memory-capped hosts (Render free tier).
#
# Excludes torch / transformers / sentence-transformers. Those are only reachable
# through CrossEncoderReranker, which imports sentence_transformers lazily inside
# its constructor — so with USE_RERANKER=false nothing in the import chain needs
# them, and the app fits in 512MB.
#
# Also excludes PyMuPDF, pandas, and streamlit: ingestion and the Streamlit UI
# are not used by app.py.

gradio==6.20.0
openai==1.58.1
chromadb==0.5.23

langchain==0.3.13
langchain-openai==0.2.14
langchain-core==0.3.28

pydantic>=2.9,<3
python-dotenv==1.0.1
langdetect==1.0.9
tiktoken==0.7.0
regex>=2024.9.11